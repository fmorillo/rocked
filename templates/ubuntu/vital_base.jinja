FROM {{ profile.baseimage }}

COPY process_monitor.py process_reporter.sh add_process.py delete_process.py /usr/local/bin/

RUN apt-get update && \
    apt-get install -y python3 python3-psutil && \
    rm -rf /var/lib/apt/lists/*

